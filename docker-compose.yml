
services:
  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=splitstream
      - POSTGRES_USER=splitstream_user
      - POSTGRES_PASSWORD=splitstream_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=splitstream
      - POSTGRES_USER=splitstream_user
      - POSTGRES_PASSWORD=splitstream_pass
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - DJANGO_SECRET_KEY=docker-dev-secret-key-change-for-real-deployment

volumes:
  postgres_data: