name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: splitstream
          POSTGRES_USER: splitstream_user
          POSTGRES_PASSWORD: splitstream_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        env:
          POSTGRES_DB: splitstream
          POSTGRES_USER: splitstream_user
          POSTGRES_PASSWORD: splitstream_pass
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
        run: python manage.py test